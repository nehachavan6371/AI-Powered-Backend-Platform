apiVersion: v1
kind: Service
metadata:
  name: llm-backend
  namespace: llm-backend
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: llm-backend
